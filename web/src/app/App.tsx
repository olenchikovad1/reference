import { Bench } from '../pages/Bench'

// Пока приложение состоит из одного экрана. Оболочка @platform/shell придёт
// вместе с входом — тогда здесь появятся маршруты и монтирование в неё.

export function App() {
  return <Bench />
}

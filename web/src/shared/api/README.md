# Клиент сервиса

Обращения к `/reference/api/`. Типы приходят из схемы, порождённой FastAPI, а не
пишутся руками вторым экземпляром: два описания одного контракта расходятся.

Токен и субъект берутся из `@platform/auth-client` — своего входа у приложения
нет (З-4).
